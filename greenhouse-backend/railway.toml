[build]
builder = "nixpacks"
buildCommand = "mvn package -DskipTests -B"

[deploy]
startCommand = "java -jar target/*.jar"
healthcheckPath = "/api/zonas"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
